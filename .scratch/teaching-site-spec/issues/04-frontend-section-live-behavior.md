# Decide how the Frontend dev example section behaves as a "living" section

Type: grilling
Status: open

## Question

The map has settled the Frontend dev example section's skeleton (two sub-threads: design research via lazyweb MCP, browser testing via Playwright MCP), framed as a living case study of this site's own build. This ticket decides the section's *behavior*, independent of which screenshots eventually fill it in: is this section meant to be updated retroactively after the site ships (so it always reflects the latest build state), or locked/frozen once the site launches? And how does the site visually signal its self-referential nature (the site documenting its own construction) to a reader — an explicit callout, a distinct visual treatment, or no special treatment at all?
